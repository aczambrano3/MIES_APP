import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.progpa.app',
  appName: 'client',
  webDir: 'dist/client',
  bundledWebRuntime: false
};

export default config;
